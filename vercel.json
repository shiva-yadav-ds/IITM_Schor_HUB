{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/chat-widget", "dest": "/api/chat-widget" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
